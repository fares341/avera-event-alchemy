import { Link } from "@tanstack/react-router";
import { ArrowRight, CheckCircle2 } from "lucide-react";
import { useInquiry } from "./site-shell";

export function PageIntro({eyebrow,title,copy,image}:{eyebrow:string;title:string;copy:string;image?:string}){return <section className="mx-auto grid max-w-[1440px] gap-10 px-5 py-14 lg:grid-cols-12 lg:px-12 lg:py-24"><div className={`${image?"lg:col-span-6":"lg:col-span-9"}`}><p className="mb-5 text-[11px] font-semibold uppercase tracking-[0.18em] text-ochre">{eyebrow}</p><h1 className="max-w-4xl text-4xl font-semibold leading-tight md:text-6xl">{title}</h1><p className="mt-6 max-w-2xl text-base leading-relaxed text-warmgray md:text-lg">{copy}</p></div>{image&&<img src={image} alt="AVERA event production" width={1280} height={900} className="aspect-[4/3] w-full rounded-lg object-cover lg:col-span-6"/>}</section>}
export function Chapter({number,label,title,children}:{number:string;label:string;title:string;children:React.ReactNode}){return <section className="mx-auto max-w-[1440px] border-t border-border px-5 py-16 lg:px-12 lg:py-24"><p className="mb-4 text-[11px] font-semibold uppercase tracking-[0.18em] text-ochre">Chapter {number} — {label}</p><h2 className="max-w-3xl text-3xl font-semibold md:text-4xl">{title}</h2><div className="mt-10">{children}</div></section>}
export function InquiryBand(){const open=useInquiry();return <section className="bg-ink text-sand"><div className="mx-auto flex max-w-[1440px] flex-col justify-between gap-8 px-5 py-14 md:flex-row md:items-center lg:px-12"><div><p className="text-xs uppercase text-camel">One team. One vision. One experience.</p><h2 className="mt-3 max-w-2xl text-3xl font-semibold">Let’s create your next event.</h2></div><div className="flex flex-wrap gap-3"><button onClick={()=>open("corporate")} className="rounded-md bg-primary px-5 py-3 text-sm font-semibold text-primary-foreground">Request Corporate Proposal</button><button onClick={()=>open("wedding")} className="rounded-md border border-sand/30 px-5 py-3 text-sm font-semibold">Wedding Consultation</button></div></div></section>}
export function BulletGrid({items}:{items:string[]}){return <div className="grid gap-px overflow-hidden rounded-lg border border-border bg-border sm:grid-cols-2 lg:grid-cols-3">{items.map(x=><div key={x} className="flex items-start gap-3 bg-card p-5 text-sm"><CheckCircle2 className="mt-0.5 size-4 shrink-0 text-ochre"/>{x}</div>)}</div>}
export function ExploreLink({to,label}:{to:"/corporate-events"|"/portfolio"|"/luxury-weddings"|"/methodology"|"/venues-production"|"/contact";label:string}){return <Link to={to} className="inline-flex items-center gap-2 text-sm font-semibold text-ochre">{label}<ArrowRight className="size-4"/></Link>}